---
- name: Install just
  homebrew:
    name: just
  when: ansible_distribution == 'MacOSX'


- name: Link just ZSH completions
  file:
    state: link
    src: "{{ dotfiles_dir }}/just/just.zsh"
    dest: "{{ ansible_user_dir }}/.oh-my-zsh/custom/just.zsh"
