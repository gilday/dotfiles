---
- name: Create Dedicated Screenshots Directory
  ansible.builtin.file:
    path: "{{ ansible_facts.user_dir }}/Desktop/sshots"
    state: directory


- name: Save Screenshots to Dedicated Directory
  community.general.osx_defaults:
    domain: com.apple.screencapture
    key: location
    value: "{{ ansible_facts.user_dir }}/Desktop/sshots"


- name: Link inputrc
  ansible.builtin.file:
    state: link
    src: "{{ dotfiles_dir }}/macos/inputrc"
    dest: "{{ ansible_facts.user_dir }}/.inputrc"




- name: Create Dracula Dir If Not Exists
  ansible.builtin.file:
    path: "{{ ansible_facts.user_dir }}/dracula"
    state: directory
    mode: 0755




- name: macOS Specific ZSH Config
  ansible.builtin.file:
    state: link
    src: "{{ dotfiles_dir }}/macos/macos.zsh"
    dest: "{{ ansible_facts.user_dir }}/.oh-my-zsh/custom/macos.zsh"


- name: Core Brew Packages
  community.general.homebrew:
    name:
      - coreutils
      - curl
      - dos2unix
      - fzf
      - gnu-sed
      - gnu-tar
      - jq
      - lastpass-cli
      - p7zip
      - ripgrep
      - tree
      - vim
      - watch
      - wget
      - z
